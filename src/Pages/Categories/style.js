import styled from 'styled-components';

export const Content = styled.div`
  width: 100vw;
  height: 100vh;
`;

export const Main = styled.main`
  padding: 2.5rem;

  h1 {
    font-weight: 500;
    font-size: 3rem;
  }

  ul {
    list-style: none;

    li {
      padding: .4rem 0;
    }
  }

  a {
    color: initial;
  }
`;